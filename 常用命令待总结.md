```java
LocalDate DEFAULT_DATE = LocalDate.of(1900, 1, 1);

List<Integer> SELL_LOCK_DAYS = Arrays.asList(-1, 30, 90, 180);


@ApiModelProperty(value = "扣款周期值")
@JSONField(name = "Period")
@JsonProperty("Period")

@JSONField(serialize = false)
@ApiModelProperty(value = "设备key")
@JsonAlias({"MobileKey", "mobileKey"})
private String mobileKey;



        EnumAccountStyle[] styles = EnumAccountStyle.values();
        for (EnumAccountStyle style : styles) {
            if (style.code.equals(code)) {
                return style.name;
            }
        }



        for (EnumPeriodType periodType : EnumPeriodType.values()) {
            if (Objects.equals(code, periodType.getCode())) {
                return periodType;
            }
        }
        return null;


    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime payTime;

    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    @JSONField(format = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime retreatInitTime;

```

